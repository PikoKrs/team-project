// Командный проект: сборник расчётов.
// Лабораторная работа № 2. Командная разработка с использованием Git.
// Команда: Максимов А. М. (в. 29), Бурбах И. Е. (в. 66), Кривов Д. Р. (в. 98, техлид).
#include <iostream>

// === БЛОК ПОДКЛЮЧЕНИЙ: каждый участник добавляет свой заголовочный файл ===
// #include "maksimov.h"
// #include "burbach.h"
#include "krivov.h"
// === КОНЕЦ БЛОКА ПОДКЛЮЧЕНИЙ ===

using namespace std;

int main() {
    int choice;

    do {
        cout << "\n=== Командный проект: сборник расчётов ===\n";
        // === БЛОК МЕНЮ: каждый участник добавляет свои пункты ===
        cout << "6. Будущая цена товара (инфляция)\n";
        cout << "7. Покупательная способность денег\n";
        // === КОНЕЦ БЛОКА МЕНЮ ===
        cout << "0. Выход\n";
        cout << "Выберите пункт: ";
        cin >> choice;

        switch (choice) {
            // === БЛОК ОБРАБОТКИ: каждый участник добавляет свои case ===
            case 6: {
                double price, inf;
                int n;
                cout << "Введите текущую цену (руб.), инфляцию (% в год) и срок (лет): ";
                cin >> price >> inf >> n;
                cout << "Будущая цена = " << futurePrice(price, inf, n) << " руб.\n";
                break;
            }
            case 7: {
                double sum, inf;
                int n;
                cout << "Введите сумму (руб.), инфляцию (% в год) и срок (лет): ";
                cin >> sum >> inf >> n;
                cout << "Покупательная способность = " << purchasingPower(sum, inf, n) << " руб.\n";
                break;
            }
            // === КОНЕЦ БЛОКА ОБРАБОТКИ ===
            case 0:
                cout << "Работа завершена.\n";
                break;
            default:
                cout << "Такого пункта нет.\n";
        }
    } while (choice != 0);

    return 0;
}
