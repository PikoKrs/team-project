// Командный проект: сборник расчётов.
// Лабораторная работа № 2. Командная разработка с использованием Git.
// Команда: Максимов А. М. (в. 29), Бурбах И. Е. (в. 66), Кривов Д. Р. (в. 98, техлид).
#include <iostream>

// === БЛОК ПОДКЛЮЧЕНИЙ: каждый участник добавляет свой заголовочный файл ===
// #include "maksimov.h"
#include "burbach.h"
// #include "krivov.h"
// === КОНЕЦ БЛОКА ПОДКЛЮЧЕНИЙ ===

using namespace std;

int main() {
    int choice;

    do {
        cout << "\n=== Командный проект: сборник расчётов ===\n";
        // === БЛОК МЕНЮ: каждый участник добавляет свои пункты ===
        cout << "4. Килограммы -> фунты\n";
        cout << "5. Фунты -> килограммы\n";
        // === КОНЕЦ БЛОКА МЕНЮ ===
        cout << "0. Выход\n";
        cout << "Выберите пункт: ";
        cin >> choice;

        switch (choice) {
            // === БЛОК ОБРАБОТКИ: каждый участник добавляет свои case ===
            case 4: {
                double kg;
                cout << "Введите массу в килограммах: ";
                cin >> kg;
                cout << kg << " кг = " << kgToLb(kg) << " фунтов\n";
                break;
            }
            case 5: {
                double lb;
                cout << "Введите массу в фунтах: ";
                cin >> lb;
                cout << lb << " фунтов = " << lbToKg(lb) << " кг\n";
                break;
            }
            // === КОНЕЦ БЛОКА ОБРАБОТКИ ===
            case 0:
                cout << "Работа завершена.\n";
                break;
            default:
                cout << "Такого пункта нет.\n";
        }
    } while (choice != 0);

    return 0;
}
